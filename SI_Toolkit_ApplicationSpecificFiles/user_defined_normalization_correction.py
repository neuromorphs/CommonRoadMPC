
def apply_user_defined_normalization_correction(df_norm_info):
    # In thie project the normalization and denormalization operations are applied by the predictor class itself

    return df_norm_info